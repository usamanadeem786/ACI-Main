{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "providers": ["python"],
    "nixpacksPlan": {
      "phases": {
        "install": {
          "dependsOn": ["setup"],
          "cmds": ["python -m venv --copies /opt/venv && ./opt/venv/bin/activate && pip install uv==$NIXPACKS_UV_VERSION && uv sync --all-extras --dev --no-install-project"]
        }
      }
    }
  }
}
